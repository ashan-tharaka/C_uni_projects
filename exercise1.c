#include<stdio.h>

void main(){

printf("An \"escape sequence\" in C language\nis a sequence of characters\n     that doesn't represent itself when used inside string \'literal\'\\ \'character\'");
}
